import { AddStyleDirective } from './add-style.directive';

describe('AddStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new AddStyleDirective();
    expect(directive).toBeTruthy();
  });
});
